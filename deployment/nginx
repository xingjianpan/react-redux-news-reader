server {
    server_name react-news;
    listen 9999;
    root /webapps/react-redux-news-reader/build;

    location / {
        try_files $uri /index.html;
        add_header   Cache-Control public;
        expires      1d;
    }
}
